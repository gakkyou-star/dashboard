# 鹿追高校ダッシュボード 引き継ぎ書

作成日: 2026-08-30（このセッションでの作業完了時点）
前回の引き継ぎ書（2026-08-28付）の内容は本書に統合済み。

## 1. これは何か

`ダッシュボード/` フォルダは、鹿追高校の学校概況・教育活動・進路・経済支援・入寮者情報をまとめた
静的HTMLダッシュボード。`scripts/*.py` が `マスターデータ/` フォルダ内の各種Excel/CSVを集計し、
`output/*.json` `.js` を生成 → `index.html` が `script.js`/`kokorozashi.js` でChart.js描画する構成。

```
ダッシュボード/
├─ index.html              全体レイアウト（左サイドバーnav + 5セクション）
├─ script.js                サマリー・学校概況・教育活動・進路・経済支援・入寮者情報の描画
├─ kokorozashi.js            探究学習アンケート（魅力化評価）分析の描画
├─ style.css                 全スタイル（ダークテーマ）
├─ scripts/
│  ├─ aggregate.py                    メイン集計スクリプト → output/dashboard_data.json/.js
│  ├─ aggregate_kokorozashi_survey.py 魅力化評価アンケート集計 → output/kokorozashi_survey_2026_tagged.json/.js
│  └─ README_探究学習アンケート.md    アンケートモジュールの更新手順・内部向け運用メモ
└─ output/                  生成物（両スクリプト実行で再生成される。直接編集しないこと）
```

**データ更新の基本コマンド**（`ダッシュボード/` で実行）:
```bash
python3 scripts/aggregate.py
python3 scripts/aggregate_kokorozashi_survey.py
```

**動作確認**: `file://` 直開き不可な場合あり。ローカルサーバー推奨:
```bash
python3 -m http.server 8000
```
→ ブラウザで `http://127.0.0.1:8000/index.html`
※ 一度開いたポートは `script.js` 等をブラウザが強くキャッシュすることがある
（`location.reload(true)` でも効かない場合あり）。更新が反映されないときは
ポート番号を変える（例: 8001）か、サーバーを再起動してから開き直すと確実。

## 2. GitHub Pages 公開（重要・新規）

このダッシュボードは GitHub Pages で一般公開されている。

- リポジトリ: https://github.com/gakkyou-star/dashboard （ユーザー: gakkyou-star）
- 公開URL: https://gakkyou-star.github.io/dashboard/
- リポジトリのルート＝ `ダッシュボード/` フォルダの中身をそのまま置く構成
  （`index.html` が直下、`style.css`/`script.js`/`kokorozashi.js`/`output/` も直下。
  サブフォルダに入れると相対パスが壊れて真っ白＆スタイル無しになるので注意）。

**このMac上のgit設定（セットアップ済み）**:
- `ダッシュボード/` フォルダ自体が git リポジトリ（`git init` 済み、リモート `origin` が
  上記GitHubリポジトリに接続済み、ブランチ `main`）。
- 認証情報は `git config --global credential.helper osxkeychain` でmacOSキーチェーンに保存済み。
  ユーザーがGitHubの個人アクセストークン（classic, `repo`スコープ）で一度手動push認証した後は、
  **Claudeが `git push origin main` するだけで追加の認証操作なしに反映できる**（実績あり）。

**今後の更新フロー**:
```bash
cd ダッシュボード
python3 scripts/aggregate.py                       # データ更新時のみ
python3 scripts/aggregate_kokorozashi_survey.py     # データ更新時のみ
git add -A
git commit -m "変更内容の要約"
git push origin main
```
push後、GitHub Pagesの反映まで数十秒〜1分程度かかることがある。

**ユーザーはGitHub操作に不慣れ**なので、「GitHubに反映して」と言われたら
上記を代行する。新しい別プロジェクトを公開したい場合は、ユーザーに
「github.com/new で空リポジトリを作成」してもらうだけで済み、認証系の
セットアップ（トークン作成・キーチェーン登録）はこのMacでは既に完了しているため
不要（詳細な経緯は本書末尾「6. 今回のセッションで解決した問題」参照）。

## 3. サイト構成（左サイドバーの順）

1. **学校概況** (`section-overview`)
2. **教育活動** (`section-activities`) — 探究学習の評価・英検・模試・魅力化評価アンケート分析を統合
   - 魅力化評価アンケート内の並び: 4資質の伸び → 大綱の政策で絞り込み → 層×資質マトリクス
     （ヒートマップ）→ 伸び幅ランキング → 県平均・全国平均との差分バブルチャート →
     アントレプレナーシップ関連指標 → **多様性・地域みらい留学の影響（新設）** → 要強化項目
3. **進路** (`section-career`)
4. **経済支援** (`section-finance`) — 支える会総会資料の「支援項目」6区分で再編済み
5. **入寮者情報** (`section-dorm`)

※ 上部タブナビは撤去済み。ナビは左サイドバー（`.side-rail`）のみ。

## 4. 主要なデータソース一覧（`マスターデータ/` フォルダ基準）

| 用途 | ファイル |
|---|---|
| 在籍生徒数（学年別） | `生徒/生徒一覧.xlsx`（シート「マージ」） |
| 入学者推移・出身地域別内訳・連携型中学校進学率 | `高校/鹿高入学者推移.xlsx`（シート「データ」） |
| 入学理由アンケート | `高校/入学理由/2026_入学理由アンケート集計.xlsx` |
| 魅力化評価アンケート（探究学習の評価・4資質・多様性セクション等） | `高校/高校魅力化評価/*.xlsx`（`aggregate_kokorozashi_survey.py`が処理） |
| 進路実績 | `高校/進路実績.xlsx`（シート「テーブル2」） |
| 英検取得状況 | `学力/英検/英検取得.xlsx` |
| 入寮者一覧 | `高校/R8鹿追高校寮入寮者一覧.csv` |
| 経済支援（予算） | `高校/R8_高校関連事業一覧_ver5.0【財政係より】.xlsx`（シート「R8_前年比」） |
| 経済支援の支援区分（支援項目）の定義元 | `管理文書/38_学校教育その他/高校関係/高校を支える会/2026総会資料/支える会資料_令和8年度.docx` |

いずれも `latest_file()` ヘルパーでglobパターン検索し、更新日時が最新のファイルを自動採用する
（ファイル名が完全一致でなくてもよい設計）。

## 5. 経済支援の集計ロジック（今回大幅刷新・要理解）

`scripts/aggregate.py` の `aggregate_financial_support()`（[aggregate.py:433](scripts/aggregate.py:433)）。

- 元データの区分（鹿追高等学校教育支援／国際バカロレア事業／その他）ではなく、
  支える会総会資料の「支援項目」6区分（`SUPPORT_CATEGORY_ORDER`、[aggregate.py:337](scripts/aggregate.py:337)）
  で再編している。
- **除外ルール**: 元Excelで内容セルの文字色が明示的に赤（`FF0000`系）の行は、
  高校支援と無関係な事業として集計から除外する（`_finance_is_red()`。openpyxlでフォント色を直接判定）。
  「国際バカロレア事業」区分は丸ごと除外。
- **行単位で分類**: 元データの「小計」バンドルは支援項目をまたぐことがある
  （例:「協力会補助事業」に全国募集の広報費と公設塾運営費が混在）ため、小計単位ではなく
  内容行単位で `FINANCE_CONTENT_CATEGORY`（[aggregate.py:347](scripts/aggregate.py:347)）に基づき分類し、
  事業ラベル単位で再集計している。**内容文字列は年度更新で変わり得るため、次年度ファイルで
  一致しなくなった項目は自動的に「未分類」区分に振り分けられ、実行時にログ警告が出る**
  （`print(f"[aggregate_financial_support] 未分類の内容が...")`）。次年度更新時はこのログを
  必ず確認し、`FINANCE_CONTENT_CATEGORY` を更新すること。
- **ユーザー指示による個別調整**（2026-08-29〜30セッションで対応）:
  - 「コーディネーター（宗雲③）」は実際は2名分の任用とのことで、金額を2倍にして計上
    （`_FINANCE_AMOUNT_MULTIPLIER`、[aggregate.py:420](scripts/aggregate.py:420)。財源内訳も同じ倍率で連動）。
  - 「学生寮運営（鹿砦寮）」の消防設備保守委託料（37千円）は削除指示により集計対象外
    （`_FINANCE_ROW_TIEBREAK`で`None`を返す行は丸ごとスキップ、[aggregate.py:401](scripts/aggregate.py:401)）。
  - ストニィプレイン町交流事業はカナダ短期留学（海外派遣事業）に合算。
  - 「協力会補助金（通学バス運行）」→「通学バス運行」、「見学旅行助成」→「入学準備・見学旅行助成」に改称。
  - 「鹿高みらい留学サポート事業」は通学環境整備→入学者対策に区分変更。
- 上記のような**追加の個別修正依頼が今後も来る可能性が高い**。パターンは共通で、
  `FINANCE_CONTENT_CATEGORY`（または`_FINANCE_ROW_TIEBREAK`/`_FINANCE_AMOUNT_MULTIPLIER`）の
  該当エントリを編集 → `python3 scripts/aggregate.py` 再実行 → 合計金額が変わらない
  （削除・倍率変更以外は）ことを確認、という流れで対応できる。

## 6. 今回のセッションで解決した問題（新しいセッションが同じ罠にはまらないように）

- **Chart.jsの`stacked: true`スケール罠**: 折れ線データセットに`stack`を明示しないと、
  同一スケール上の他の未指定折れ線と暗黙に積み上げられてしまう（例: 「総数」線と「町外」線が
  合算された位置に描画される不具合が実際に発生した）。複数の折れ線を同じ棒グラフに重ねる時は
  各線に固有の`stack`名を必ず付けること。
- **CSSグリッドの`fr`単位罠**: `grid-template-columns: 1.6fr 1fr` のように裸の`fr`を使うと、
  中身（特に幅の広いグラフ）のmin-content幅未満に縮まらず、タブレット幅で横はみ出しする
  （iPadで実際に発生・報告された）。2カラム以上のグリッドで中に可変幅コンテンツを置く場合は
  必ず `minmax(0, 1.6fr) minmax(0, 1fr)` のように `minmax(0, ...)` で包むこと。
- **Chart.js凡例は既定で中央揃え**: `legend: { position: "bottom" }` だけだと項目テキスト長が
  違う場合に左端が揃わない。`align: "start"` を追加すると左揃えになる（script.js全8箇所に適用済み）。
  新しい凡例付きグラフを追加する際もこれを踏襲すること。
- **GitHub Pagesトラブル**: 当初`index.html`しかアップロードされておらず、CSS/JS/データが
  すべて404で真っ白（文字だけ）になっていた。「GitHubに公開した」＝ファイル一式が揃っているとは
  限らない点に注意。
- **ブラウザのJSキャッシュ**: 同じ`http://127.0.0.1:PORT/`で検証を繰り返すと、`script.js`の
  変更が反映されないことがある（`location.reload(true)`も効かない場合あり）。挙動がおかしい時は
  まずポート番号を変えて開き直し、切り分けること。
- **プレビュー機能のスクリーンショットが低頻度で不安定**: スクロール後にスクリーンショットが
  真っ黒/真っ暗になることがあったが、`get_page_text`や`elementFromPoint`、Chart.jsの内部状態
  （`chart.legend.legendHitBoxes`等）をJS経由で直接確認すると実際には正常描画されていた
  （ツール側の一時的な問題で、ページの不具合ではなかった）。スクリーンショットが不審な時は
  これらの代替手段で裏取りするとよい。

## 7. 設計上のパターン・流用できる共通部品

- **`renderStatCards(containerId, cards)`**（`script.js`）: アイコン付きKPIタイルの共通描画関数。
- **`.matrix-table` → 廃止済み**。都道府県マトリックスは `.dorm-matrix-pref-card`
  （都道府県カード＋学年別ミニバー）に置き換え済み。ヒートマップ表よりバー表現の方が
  「一目で分かる」というユーザー要望に沿った結果。同様の「表よりバー」方針は今後も踏襲するとよい。
- **`.funnel-list`/`.funnel-row`**: 2カラムのバーリスト（伸びている項目／課題の項目等）。
  `.funnel-label`と`.funnel-value`はどちらも`min-height: 2.6em`＋`line-height`明示で
  「2行分の高さを確保」し、左右カラム・行内でバー位置を揃えている。値が1行/2行で高さが変わる
  項目が混在する場合は`.funnel-value`側にも同じ手法が要る（実際に一度ズレて修正した）。
  `.funnel-list`のgapは26px、`.funnel-label`のmargin-bottomは6pxで意図的に非対称にし、
  「どのバーがどの設問のものか」を視覚的に分かりやすくしている。
- **`.diversity-legend`**（多様性セクション用に新設）: 色ドット＋テキスト＋右寄せpt値の
  シンプルな独自凡例。Chart.js内蔵凡例だと長い設問文が入りきらないため、HTML側で別途
  16px文字の凡例を作り、Chart.js側は`legend:{display:false}`にする、というパターン。
  今後も「設問文が長い」ケースではこの方式が使える。
- **`el(tag, className, textContent)`**（`script.js`）: DOM生成の共通ヘルパー。
- Chart.jsのグローバルデフォルト（フォントサイズ13px等）は `script.js` 冒頭で一括設定済み。
- 政策タグ絞り込み（`activePolicyFilter`/`matchesPolicyFilter`/`setPolicyFilter`）は
  `kokorozashi.js`内で完結。新しいセクションを追加してタグ絞り込みに連動させたい場合は、
  ①そのセクションの`policy_tags`を持たせる、②`matchesPolicyFilter`でフィルタする、
  ③`setPolicyFilter()`内の呼び出しリストに再描画関数を追加、の3点が必要
  （多様性セクションで実装済み。カード全体を`.policy-scope-dimmed`でopacity 0.4にする方式）。
- 参考: `管理文書/50_その他/視察資料/高校支援/data/aggregate.py` は同種の集計ロジックの姉妹プロジェクト。

## 8. 未確認・要フォローアップ

- **出身地別内訳（住所→都道府県推測）**: ユーザーからまだファイルが提供されていない。
  `aggregate.py` の `aggregate_school_overview()` 内に TODO コメントを残してある。
  ファイルが `高校/` フォルダに追加されたら、`infer_prefecture()`（`aggregate_dormitory()` で
  使用中の同名関数）を流用して実装する。
- **経済支援の`FINANCE_CONTENT_CATEGORY`マッピング**: 次年度データで内容文字列が変わった場合、
  「未分類」区分に落ちるので実行時ログを確認して更新すること（詳細は本書5節）。
- **「多様性・地域みらい留学の影響」セクション**: 現行アンケートに「地域みらい留学生／地元生」の
  属性区分がなく、相関の提示にとどめている。次年度以降、属性区分付きアンケートが実現したら
  `aggregate_kokorozashi_survey.py`の`diversity_impact`ブロックを拡張する
  （詳細は`scripts/README_探究学習アンケート.md`内の運用メモを参照）。
- レスポンシブ確認はモバイル幅（363px）・タブレット幅（768〜1194px、iPad各種）・
  デスクトップ幅（1400px相当）で目視確認済み。
