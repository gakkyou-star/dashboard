# 鹿追高校ダッシュボード 引き継ぎ書

作成日: 2026-08-28（このセッションでの作業完了時点）

## 1. これは何か

`ダッシュボード/` フォルダは、鹿追高校の学校概況・教育活動・進路・経済支援・入寮者情報をまとめた
静的HTMLダッシュボード。`scripts/*.py` が `マスターデータ/` フォルダ内の各種Excel/CSVを集計し、
`output/*.json` `.js` を生成 → `index.html` が `script.js`/`kokorozashi.js` でChart.js描画する構成。

```
ダッシュボード/
├─ index.html              全体レイアウト（左サイドバーnav + 6セクション）
├─ script.js                サマリー・学校概況・教育活動・進路・経済支援・入寮者情報の描画
├─ kokorozashi.js            探究学習アンケート分析（教育活動セクションに統合済み）の描画
├─ style.css                 全スタイル（ダークテーマ）
├─ scripts/
│  ├─ aggregate.py                    メイン集計スクリプト → output/dashboard_data.json/.js
│  └─ aggregate_kokorozashi_survey.py 探究学習アンケート集計 → output/kokorozashi_survey_2026_tagged.json/.js
└─ output/                  生成物（両スクリプト実行で再生成される。直接編集しないこと）
```

**データ更新の基本コマンド**（`ダッシュボード/` で実行）:
```bash
python3 scripts/aggregate.py
python3 scripts/aggregate_kokorozashi_survey.py
```

**動作確認**: `file://` 直開き不可な場合あり。ローカルサーバー推奨:
```bash
python3 -m http.server 8000
```
→ ブラウザで `http://127.0.0.1:8000/index.html`

## 2. サイト構成（左サイドバーの順）

1. **学校概況** (`section-overview`)
2. **教育活動** (`section-activities`) — 探究学習の評価・英検・模試・探究学習アンケート分析を統合
3. **進路** (`section-career`) — 「進路・学習」から改称、学習系カードは教育活動へ移動済み
4. **経済支援** (`section-finance`) — 「財政支援」から改称
5. **入寮者情報** (`section-dorm`)

※ 上部タブナビ（`.tab-nav`）は撤去済み。ナビは左サイドバー（`.side-rail`）のみ。

## 3. このセッションで行った作業（新しいセッションはここから続きを担当する）

### 3-1. サイト構造の大改修（`修正指示書/修正01...md` の指示による）
- 上部タブ削除、左サイドバーのみに
- 学校概況: 進路決定率カードを削除／「連携型中学校からの進学率」を折れ線グラフ化／
  「在籍生徒の町内割合」（現1〜3年生の入学時内訳、円グラフ）を新設／
  「入学者数の推移」を町内・管内・道内・道外の積み上げ棒＋総数線＋町外線の複合グラフに統合し
  2023年度（地域みらい留学制度加盟）に分岐点マーカー表示／入学理由・支援内容を割合(%)表示に変更
- `origin_region`/`admission_trend`/`current_enrollment_origin` は
  `高校/鹿高入学者推移.xlsx` の **「データ」シート**（1986〜最新年度まで完備）から一括導出するよう統一。
  旧「入学者内訳」シートは2022〜2025年度分のみで不完全なため使用をやめた。
- 教育活動: 「探究学習アンケート」独立セクションを統合。英検・模試カードも移動。
- 進路: 「進路・学習」→「進路」に改称。決定率(毎年100%で無意味)と魅力化アンケートのレーダーチャートを削除。
- 入寮者情報: 視察資料側 (`管理文書/50_その他/視察資料/高校支援/data/aggregate.py`) の
  施設別×都道府県×学年×性別マトリックス集計ロジックを移植。

### 3-2. 未着手・保留中の項目
- **出身地別内訳（住所→都道府県推測）**: ユーザーからまだファイルが提供されていない。
  `aggregate.py` の `aggregate_school_overview()` 内に TODO コメントを残してある。
  ファイルが `高校/` フォルダに追加されたら、`infer_prefecture()`（`aggregate_dormitory()` で使用中の
  同名関数）を流用して実装する。

### 3-3. 入寮者情報のインフォグラフィック化（ユーザー要望）
「一目でわかるように」との要望で以下を追加：
- 統計KPI行（総入寮者数／全体在寮率／出身都道府県数）— `renderStatCards()` 共通ヘルパーを新設し再利用
- 施設別カードに定員に対する在寮率ドーナツゲージ、学年別ミニバー、
  **1人＝1ドットのピクトグラム**（男=青／女=赤）
- 都道府県×学年×性別マトリックスをセルの濃淡で表現するヒートマップテーブルに変更
- 都道府県別内訳に🥇🥈🥉メダルと男女2色の積み上げバー
- （ユーザー指摘で2件バグ修正済み）ゲージ中央の数字がリンクからズレる問題（`align-items:baseline`→`center`）

### 3-4. 探究学習アンケートの政策タグ統一（追記指示書_政策タグ統一.md）
- 大綱「基本的な政策（政策1〜政策15）」に全タグを統一。旧 `policy2_innovation` 等のキーは全廃。
- `scripts/aggregate_kokorozashi_survey.py`:
  `CATEGORY_POLICY_TAGS`（資質×層のデフォルトタグ）、`QUESTION_POLICY_TAGS`（個別設問の重点タグ、
  カテゴリタグより優先）を新設。`POLICY_LABELS` を単一の正として `kokorozashi.js` 側もこれを参照
  （JS側のハードコード辞書は廃止）。
- 「大綱の政策で絞り込み」フィルターUIを新設（教育活動セクション、ヒートマップの直前）。
  ヒートマップの減光・要強化項目リスト・アントレプレナーシップ指標リストが連動して絞り込まれる。
- （ユーザー指摘で2件バグ修正済み）政策タグpillのCSSが `.weak-card` 配下限定になっていて
  ヒートマップ詳細パネルでは無地文字が連続する不具合、および長いラベルで楕円が歪む不具合
  （`border-radius:999px`→`var(--radius-sm)`）、政策番号順ソート未対応。

### 3-5. 進路先カテゴリ別人数の推移（追記指示書_進路チャート改善.md）
- 「進路実績の推移」（卒業者数とほぼ同義で無意味だった合計棒グラフ）を、
  進路先カテゴリ別（専門学校／就職／私立大／国公立／その他／短大）の複数系列折れ線グラフに置換。
- データソース `高校/進路実績.xlsx` の「テーブル2」シートに2012〜2025年度の年度×進路区分データが
  完備していたため、そのまま集計。「高看」「留学」区分は指示書の6区分に含まれないため「その他」に統合
  （`CAREER_CATEGORY_ALIASES`）。
- 色はドーナツグラフ（左カラム、変更なし）と揃うよう `QUALITATIVE_PALETTE` の先頭6色を固定順で割当。
- 凡例クリックでの系列表示切替はChart.js標準機能でそのまま動作。

### 3-6. 経済支援（旧・財政支援）の実データ化
- 新ファイル `高校/R8_高校関連事業一覧_ver5.0【財政係より】.xlsx`（「R8_前年比」シート）を集計。
- 「小計」「計」行をマーカーとして区分（3つ）→事業（23件）の階層データに変換。
  各階層の合計が上位合計と完全一致することを検証済み（総額290,580千円）。
- 財源内訳（国・道・地方債・その他・特別交付税・一財）も集計し、一般財源比率を算出。
- **重要: 元データのラベル付け誤りを補正済み**。「シェアハウス事業」小計に
  Penguin Dormitory関連項目（寄宿舎燃料費・水道代・管理運営業務委託料等）が混入していたため、
  内容欄に「寄宿舎」「Dormitory」が複数出現する場合は「高校寄宿舎事業（Penguin Dormitory）」に
  自動補正するロジックを `_finance_close_project()` に実装（コメントで理由明記）。
  **次年度以降、元データの列構成やラベル付けが変わった場合はこの補正ロジックの妥当性を再確認すること。**
- 同一区分内で事業名が重複する場合（例:「協力会補助事業」が2件）は内容欄の括弧内語句で自動区別
  （`_finance_dedupe_labels()`）。
- 表題を「財政支援」→「経済支援」に変更（`index.html` の見出し・サイドバーのtitle属性）。

## 4. 主要なデータソース一覧（`マスターデータ/` フォルダ基準）

| 用途 | ファイル |
|---|---|
| 在籍生徒数（学年別） | `生徒/生徒一覧.xlsx`（シート「マージ」） |
| 入学者推移・出身地域別内訳・連携型中学校進学率 | `高校/鹿高入学者推移.xlsx`（シート「データ」） |
| 入学理由アンケート | `高校/入学理由/2026_入学理由アンケート集計.xlsx` |
| 魅力化評価アンケート（探究学習の評価・4資質レーダー元データ） | `高校/高校魅力化評価/*.xlsx` |
| 探究学習アンケート詳細（政策タグ・ヒートマップ等） | `高校/高校魅力化評価/*.xlsx`（同上、`aggregate_kokorozashi_survey.py`が別途処理） |
| 進路実績 | `高校/進路実績.xlsx`（シート「テーブル2」） |
| 英検取得状況 | `学力/英検/英検取得.xlsx` |
| 入寮者一覧 | `高校/R8鹿追高校寮入寮者一覧.csv` |
| 経済支援（予算） | `高校/R8_高校関連事業一覧_ver5.0【財政係より】.xlsx`（シート「R8_前年比」） |

いずれも `latest_file()` ヘルパーでglobパターン検索し、更新日時が最新のファイルを自動採用する
（ファイル名が完全一致でなくてもよい設計）。

## 5. 設計上のパターン・流用できる共通部品

- **`renderStatCards(containerId, cards)`**（`script.js`）: アイコン付きKPIタイルの共通描画関数。
  `summary-grid` クラスのコンテナに対して使う。新しいセクションにKPI行を足す時はこれを再利用する。
- **`.matrix-table`**: ヒートマップ的な色分けテーブルの共通クラス（入寮者情報の都道府県マトリックス等）。
- **`sortedPolicyTags()` / `matchesPolicyFilter()`**（`kokorozashi.js`）: 政策タグの表示順・絞り込みの共通ロジック。
- **`el(tag, className, textContent)`**（`script.js`）: DOM生成の共通ヘルパー。innerHTML直書きより優先して使う。
- Chart.jsのグローバルデフォルト（フォントサイズ13px等）は `script.js` 冒頭で一括設定済み。
  個別チャートで上書きしない限りこれに従う。
- 参考: `管理文書/50_その他/視察資料/高校支援/data/aggregate.py` は同種の集計ロジック
  （入寮者マトリックス、予算のman-oku表記フォーマッタ等）を先に実装済みの姉妹プロジェクト。
  流用できるロジックがないか、新しい集計要件が出たらまず覗くとよい。

## 6. 未確認・要フォローアップ

- 出身地別内訳（住所→都道府県推測）: ファイル待ち（3-2参照）
- 経済支援のラベル補正ロジック（3-6参照）: 次年度データで要再検証
- レスポンシブ確認は主にモバイル幅（363px）と1400px相当のデスクトップ幅で目視確認済み。
  中間幅（タブレット等）は未確認。
